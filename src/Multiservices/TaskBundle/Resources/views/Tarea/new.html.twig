{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tarea creacion</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tarea') }}">Regresar a la lista</a>
        </li>
    </ul>
{% endblock %}

